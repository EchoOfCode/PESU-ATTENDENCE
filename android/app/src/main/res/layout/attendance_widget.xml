<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="8dp"
    android:background="#00000000">

    <!-- All dynamic content is injected into this rounded container -->
    <LinearLayout
        android:id="@+id/widget_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/widget_background"
        android:clipToPadding="false">

        <!-- Section 1: Header row (date + last updated) — injected dynamically -->
        <!-- Section 2: Next class row — injected dynamically -->
        <!-- Section 3: Subject list — injected dynamically -->
        <!-- Section 4: Academic dates — injected dynamically -->
        <!-- Section 5: Funny quote footer — injected dynamically -->

    </LinearLayout>

</LinearLayout>
